---
import '../styles/global.css'

interface Props {
    title?: string;
    showBackLink?: boolean;
}

const { title, showBackLink = true } = Astro.props;
const pageTitle = title ? `${title} | Garage` : 'Garage';
const isTopPage = !title;
---

<!doctype html>
<html lang="en" class="h-full w-full m-0">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width" />
        <title>{pageTitle}</title>
    </head>
    <body class="h-full w-full m-0">
        <slot />
        {showBackLink && !isTopPage && (
            <div class="fixed bottom-6 left-6">
                <a
                    href="/"
                    class="inline-flex items-center gap-2 px-4 py-2 text-sm font-medium text-slate-600 transition-colors hover:text-slate-900 hover:shadow-lg"
                >
                    ← Back to Top
                </a>
            </div>
        )}
    </body>
</html>

